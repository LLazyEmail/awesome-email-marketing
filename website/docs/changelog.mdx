---
title: "Changelog"
description: "What changed in the Awesome Email Marketing website and library."
keywords:
  - changelog
  - release notes
  - meta
  - awesome email marketing
  - email marketing
sidebar_label: "Changelog"
sidebar_position: 3
image: img/docusaurus-social-card.jpg
hide_title: false
hide_table_of_contents: false
toc_min_heading_level: 2
toc_max_heading_level: 3
draft: false
unlisted: false
category: meta
topics:
  - meta
audience:
  - contributors
seo:
  title: "Changelog | Awesome Email Marketing"
  description: "What changed in the Awesome Email Marketing website and library."
  robots: index,follow
  canonical_path: "/docs/changelog"
  og_type: article
  twitter_card: summary_large_image
---

import changelog from '@site/src/data/changelog.json';

# Changelog

Notable updates to the documentation site and curated library.

Entries are generated automatically from merged pull requests via [`auto-changelog`](https://www.npmjs.com/package/auto-changelog). Run `npm run changelog` in `website/` locally, or rely on the **Update changelog** GitHub Action after each merge to `main`.

{changelog.map((entry) => (
  <section key={`${entry.date}-${entry.title}`}>
    <h2>
      {entry.date} — {entry.title}
    </h2>
    <ul>
      {entry.items.map((item) => (
        <li key={item}>{item}</li>
      ))}
    </ul>
  </section>
))}
