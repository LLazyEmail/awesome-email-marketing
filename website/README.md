# Awesome Email Marketing website

Docusaurus site for the Awesome Email Marketing list.

## Local development

```bash
cd website
npm install
npm start
```

Open [http://localhost:3000/awesome-email-marketing/](http://localhost:3000/awesome-email-marketing/).

Useful commands:

- `npm start` — local development server
- `npm run build` — production build
- `npm run serve` — serve the production build locally

## Search

Local search is enabled with [`@easyops-cn/docusaurus-search-local`](https://github.com/easyops-cn/docusaurus-search-local). Use the search box in the navbar or press `Ctrl/⌘ + K`.

## Explore, API, glossary

- `/explore` — tag/filter UI over the curated catalog
- `/docs/glossary` — email marketing glossary
- `/docs/changelog` — site/library changelog (auto-generated from merged PRs)
- `/docs/api` — public JSON API docs
- `/api/v1/catalog.json` — machine-readable catalog

Regenerate catalog exports:

```bash
npm run generate:api
```

Regenerate the changelog (root `CHANGELOG.md` + `src/data/changelog.json`):

```bash
npm run changelog
```

Uses the [`auto-changelog`](https://www.npmjs.com/package/auto-changelog) npm package. Config lives in [`.auto-changelog`](./.auto-changelog).

## Automatic deployment

GitHub Actions workflows live in [`.github/workflows/`](../.github/workflows):

- `test-deploy.yml` — builds the site on pull requests
- `deploy.yml` — builds and deploys to GitHub Pages on pushes to `main`
- `changelog.yml` — regenerates `CHANGELOG.md` and the docs changelog after each merged PR to `main`

### One-time GitHub Pages setup

1. Open the repository **Settings → Pages**
2. Under **Build and deployment**, set **Source** to **GitHub Actions**
3. Merge a PR (or run **Deploy to GitHub Pages** manually)

The site will be available at:

`https://llazyemail.github.io/awesome-email-marketing/`

## Content layout

```text
docs/
  intro.md                 Overview / start here
  glossary.mdx / api.mdx / changelog.mdx
  tools/
    best-tools.md          Hub page
    newsletter-platforms.md
    self-hosted-esp.md
    email-builders.md
    deliverability-tools.md
    growth-automation-tools.md
    email-service-providers.md
    ecommerce.md / automation-platforms.md / ...
  strategy/                Growth and campaign strategy
  guides/                  Reading lists + vendor-blogs/
  development/             HTML email and GitHub projects
  operations/              GDPR, DNS/DKIM, SES, metrics/
  more/                    Transactional, services, extras
```

The docs sidebar is **fully explicit** in `sidebars.ts` (not filesystem-autogenerated). Update that file whenever pages are added or reordered.

Docs pages include advanced SEO frontmatter (`description`, `keywords`, `topics`, `audience`, `seo.canonical_path`, `seo.robots`, social card fields) for later SEO work.